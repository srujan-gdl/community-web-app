# Copy this to .env and fill in the values.
# This file is safe to commit — it contains NO secrets.

# API base URL — update this when deploying to staging or production
VITE_API_URL=http://localhost:4000
